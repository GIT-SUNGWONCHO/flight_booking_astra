# API 예매 개발 계획 — 클로드 실행 원본

최신 검증(2026-09-14 16:37): [일반석 연속 리허설 통과](rehearsal-2026-09-14-1637.md), runId b566b85f58a4. 달력 준비→API→같은 주문 브라우저→실제 Npay까지 중간 개입 없이 완료했다. 전송T0+3.046초·응답9.384초·Npay30.275초, 최종 결제 안 함. 아래 이전 시점의 ‘연속 완주 미검증’은 이 노선 일반석1회에 한해 갱신됐다. 09시 신규 프레스티지·실제 좌석 확보 시각·다른 방향·OS 재부팅 조건은 미검증이다. 운영 병합/자동 발사 전환 없음. 과거 문서는 `../docs/archive/2026-09-14/historical-api/`에 보존했다.

기준: 2026-09-13 사용자 재확정. **이 문서 §3은 전체 처리 순서, §4는 개발 순서, §5는 시험 통과 조건이다.** 현재 첫 작업은 [NOW](../NOW.md), 시작 프롬프트는 [§8](#claude-prompt). 과거 A1~A8 기록의 ‘다음 작업’은 적용하지 않는다.

## 1. 목표와 완료의 의미

- 시작: 정상 로그인된 브라우저의 **달력 화면에서 목표 예매를 시작할 수 있게 준비**한다. 어제 주문/결제 게이트에 머물러야만 API가 동작하는 구조는 완료가 아니다.
- 성능: 개방 기준 T0부터 목표 주문의 임시 보유까지 단축. 주문 전송 ≤3초는 도전 목표이며, 사용자에게 6~7초 확보도 의미가 있다. 둘은 다른 지표다.
- 기능: API 조회·운임·필수 검증·주문 후 **같은 주문**의 브라우저 동의·마일리지 적용·Npay 선택·실제 Npay 창까지 연결한다. 최종 승인은 사용자만 한다.
- 브라우저는 처음부터 살아 있다. 1회 인계는 중간 화면의 렌더링/클릭 대기를 줄인다는 뜻이며, 페이지별 클릭 재생을 모두 강제하거나 정상 상태 갱신을 생략한다는 뜻이 아니다.
- 사용자 설명에 따라 pnr의 임시 보유 의미를 반영한다. 다만 전송 시각·pnr 응답 시각·실제 서버 보유 시작 시각·해제 시각은 구분한다. 정확한 시작/기한은 미확인이다.
- 일반석 가상 T0와 실제 09시를 구분한다. 5초 매진 마감·9.4초 구조적 하한·주문 왕복 전부가 단축 불가능한 서버 처리라는 주장은 사용하지 않는다. 운영 시계는 수정하지 않는다.

## 2. 현재 구현과 근거

| 영역 | 파일 | 확인 범위 / 남은 연결 |
|---|---|---|
| A1 상태 모델 | [order_flow.py](order_flow.py) | 단일 객체 전이 로컬 시험7개. 프로세스 간 잠금·서버 중복 방지 아님 |
| A2 조회 / A3 운임 | [availability.py](availability.py), [fare.py](fare.py) | 합성 계약 로컬8/10개. 최신 식별자·여정 검증을 실제 전송 경로에 연결할 필요 |
| A4 회원/승객/주문 판정 | [eligibility.py](eligibility.py), [travellers.py](travellers.py) | 로컬8/23개. 실제 회원 증거·연락처 구조·응답 차이 미반영 영역 있음 |
| A5/A6 결제·인계 모델 | [payment.py](payment.py), [handoff.py](handoff.py) | 로컬24/22개. 실제 화면/결제창을 여는 구현 아님 |
| A7/A8 실전송 경로 | [transport.py](transport.py), [live_order.py](live_order.py), [site_drive.py](site_drive.py) | 일반석 API 주문 응답 관측. 달력 시작·동일 주문 인계·Npay 전체 성공은 미입증 |
| 실전송 로직 시험 | [test_live_order.py](test_live_order.py) | 로컬14개. 위 오프라인 모델의 검증이 전부 연결됐다는 증거 아님 |
| 수집 / 분석 | [collect.py](collect.py), [contracts.py](contracts.py), [evidence.py](evidence.py), [analyze.py](analyze.py) | 제한된 구조·시각 기록. 허용 목록은 확정 API 계약이 아님 |

시험 수는 과거 실행 기록이며 이번 문서 작업에서 재실행하지 않았다. API 관련120개 집계에는 analyze4가 포함되고 수집기 시험은 별도다. 파일별 명령·당시 해시·실패/수정 이력은 [개발 보관본](../docs/archive/2026-09-13/before-api-README.md)에 있다.

- 9/13 미발사·5회 일반석 실측·사용자 결정: [사건 기록](../docs/archive/2026-09-14/historical-api/live-attempt-2026-09-13.md), [현재 사실](../FACTS.md#api-evidence).
- 직전 정적 검토: [084941 검토](../dev-shots/reviews/20260913-084941-c1010a07/review.md). 게이트 동일성·배타 전송·늦은 무장·결제 성공 오판 지적.
- [9/12 검토](../docs/archive/2026-09-14/historical-api/feasibility-review-2026-09-12.md)는 당시 판단이다. 보유 의미의 사용자 정정·이후 실전송 관측은 FACTS가 현재 원본이다.
- `deadline.py`/과거 budget.json은 잘못된 마감 가정이 있어 성능·출시 판정에 사용 금지. 이번 우선 개발에 포함하지 않는다.

<a id="process"></a>
## 3. 전체 API 예매 프로세스 — 순서 유지

| 순서 | 처리 | 통과 조건 |
|---|---|---|
| 1 | 브라우저 사전 준비 | 본인 정상 로그인·KRW·승객/연락처·목표·세션. 달력 상태에서 실행 가능. 준비 중 주문 여부를 별도 식별 |
| 2 | 사용자 대기 시작 | 목표/설정/코드/세션 확인. 변경 시 준비 무효화. 지나간 발사 시각은 자동 실행 금지 |
| 3 | awardAvailability | 개방 후 목표 전체 날짜·출발지·도착지로 조회. 미개방/오류/매진 구분 |
| 4 | 목표 선택 | 날짜·노선·운항사·편명·등급·재고 검증. 같은 최신 응답의 recommendId/flightId 선택 |
| 5 | fareInformation | 최신 식별자로 운임 요청. 여정·등급·KRW·금액·필요 마일리지·pageTicket/세션 문맥 검증 |
| 6 | 필수 회원·마일리지·세션 검증 | 재사용 가능한 사전 증거만 사용. 새 필요액과 실제 가용 잔액 비교, 승객·연락처·회원 적격 확인 |
| 7 | inputTravellers 1회 | 최신 운임 문맥 일치, 검증 완료, 전송권 획득과 의도 기록 이후 전송 |
| 8 | 동일 주문 상태 확인 | pnr뿐 아니라 목표 여정·등급·업무 상태 대조. 응답 불명확은 ORDER_UNKNOWN, 재전송 금지 |
| 9 | 브라우저 1회 인계 | 현재 API 주문과 화면 주문 식별자 연결. 검색/운임/승객/금액 상태 일관성, 새 주문 생성 없음 |
| 10 | 동의·마일리지·결제수단·결제창 | 기존 브라우저 방식. 각 동의 완료, 실제 마일리지 적용, Npay 선택, 새 실제 제공자 창 확인 |
| 11 | 최종 결제 승인 | 사용자만 수행 |

같은 로그인 Chrome의 정상 요청 문맥을 사용한다. 인증·접근 대기·필수 검증을 생략하지 않는다. 최신 조회에 의존하는 운임을 추측으로 병행하지 않고 과거 식별자로 대체하지 않는다. 정상 인계에 필요한 **같은 주문 읽기**는 허용하며 별도 계측한다. 주문 재생성은 인계 방법이 아니다.

<a id="development"></a>
## 4. 개발 보완 순서 — 한 번에 한 가지

D1~D5는 새로 합의한 보완 작업명이다. A1~A8을 처음부터 다시 만드는 계획이 아니다. D1의 계약 조사·로컬 모델은 선행 가능하지만 **실제 주문 시험은 D3/D4까지 갖춘 뒤 §5에서만** 수행한다.

| 단계 | 구현 범위 | 로컬 완료 조건 |
|---|---|---|
| **D1 첫 작업: 동일 주문 인계 계약** | 사이트 정상 주문 응답 처리/store/action/라우팅 또는 정상 같은 주문 재개 경로 조사. 기존 handoff/site_drive에 최소 연결 모델 추가 | 새 주문과 이전 주문 구분. 같은 날짜·마일리지라도 다른 pnr은 거부. 새 주문 요청을 유발하는 인계는 실패 |
| D2 달력 시작 준비 | 페이지 수명과 요청 문맥 보존을 정리하고 달력에서 API 시작 가능하게 연결 | 달력 복원/문서 전환 뒤 필요한 문맥 유지 또는 명확한 재준비. 주문 페이지 고정 의존성 제거. 준비/조회/주문 구간 호출 수 구분 |
| D3 실제 계약과 검증 연결 | 기존 A2~A4와 실전송 경로의 입력·응답 차이를 대조하고 필요한 판정을 연결 | 잘못된 날짜/노선/편명/등급, 매진/미개방/업무 오류, 잔액 부족/세션 만료에서 주문0회 |
| D4 주문 전송 안전성 | 실제 전송 직전 배타 전송권·의도 기록·불명확 복구·늦은 무장 차단 | 두 프로세스 동시 시작 중 하나만 전송. 중단/응답 유실/재시작 후 새 주문 차단. Windows 시험 |
| D5 브라우저 후반 연결 | 동의→마일리지→Npay 선택→실제 Npay 판정을 기존 정상 단계와 연결 | 체크된 동의를 다시 꺼버리지 않음. 실패 전파. 카드 PG·로그인·이전 창·금액 불일치는 완료 아님 |

### D1에서 바로 할 일

1. `transport.py`, `live_order.py`, `site_drive.py`, `handoff.py`와 9/13 최종 검토를 대조한다. 기존 판정 모델을 재구현하지 않는다.
2. 보존된 사이트 소스/수집에서 주문 응답→정상 화면 상태→게이트의 연결 위치를 찾는다. 파일·함수·필드·호출 순서·페이지 이동 부작용을 기록한다. 자료가 없으면 없는 항목을 명시한다.
3. 우선은 정상 상태 갱신을 유지하고 화면을 인계하는 경로, 다음 후보는 정상 같은 주문 조회/재개 경로다. 단순 `goto(GATE)`·성공 플래그 주입은 확정 경로가 아니다.
4. `Order.reference`와 실제 화면이 사용하는 주문 참조를 메모리에서 비교할 방법을 계약으로 정한다. 날짜·마일리지·KRW 일치만으로 대체하지 않는다. 식별자 원문은 로그에 쓰지 않는다.
5. 확인된 계약만 최소 구현·가짜 브라우저/전송기로 시험한다. 실제 계약이 부족하면 합성을 실증으로 승격하지 말고 **다음에 수집할 한 가지 증거**를 특정해 남긴다.

### 개발 중 빠뜨리면 안 되는 항목

- **회원/선택 문맥:** inputTravellers 본문에 여정이 없다는 것만으로 전달 메커니즘을 확정하지 않는다. fare 결과·pageTicket·쿠키/헤더·서버 선택이 목표와 이어짐을 확인한다. 다른 탭/작업의 선택 변경도 고려한다.
- **계약 차이:** 현재 travellers 합성 모델은 연락처1항목, 실측은 contactList2항목. 실제 허용 구조로 보완한다. 캡처 일반석/이전 날짜→새 날짜 프레스티지 조합·travellerId 수명·본인 잔액을 검증한다.
- **사전 처리:** 가족정보/회원검증을 속도 때문에 무조건 생략하지 않는다. 캐시의 입력 키·유효 조건·강제 갱신·여정 의존성을 확인한다. 현재 본인 예매 범위를 가족 합산으로 임의 확장하지 않는다.
- **준비 부작용:** capture_pass/replay_probe는 캡처만으로 임시 주문을 만들 수 있다. live_order의 `--dry`도 캡처까지 무주문으로 보장하지 않는다. 주문 없는 준비 방법을 우선 조사하고, 불가하면 생성 건수/상태를 명시해 시험 범위를 구분한다.
- **페이지 수명:** add_init_script 등록, 현재 문서의 캡처, 브라우저 연결 재생성은 다른 상태다. 단순 complete/존재/캡처 나이를 로그인 유효성이나 화면 준비 완료로 해석하지 않는다. 헤더 timestamp의 보편적 유효기간을 30분 표본 하나로 정하지 않는다.
- **오류:** HTTP200/JSON/pnr 존재/exit0만으로 성공 금지. 전송 후 응답 불명확·잘못된 여정·업무 오류는 서버 주문 부재를 뜻하지 않는다. 정상 조회에서 안 보인다는 사실만으로 재전송 허용 금지.
- **후반 UI:** 동의는 두 모달 각각 상태·스크롤·완료를 확인한다. Npay가 안 되면 다른 PG로 대체하지 않는다. 마일리지 적용액·최종 KRW 금액·현재 주문을 대조한다.
- **현재 금지:** `--continue-payment`는 결함 수정·관련 검증 전 사용 금지. `--gate-only`는 동일 주문 보장 없음. 로컬 intent의 resolved는 서버 해제 증거가 아니다. 재실행을 위해 파일을 임의 삭제하지 않는다.
- **범위:** 연구 기본9242, 과거9232 예외는 AGENTS 참조. 운영9232/계측9233·시계·사용자 대기·일정 자동 변경 금지. 계측 실패가 예매를 중단시키지 않는다.

<a id="testing"></a>
## 5. 시험 단계와 실전 진입 조건

| 순서 | 시험 범위 | 통과 조건 |
|---|---|---|
| T1 단계별 로컬 | fixture/가짜 전송/가짜 화면, D4 프로세스 시험 | 정상·반례·중단을 확인. 해당 파일 시험과 영향받는 회귀 실행. 최종 기준은 회사 Windows |
| T2a 주문 전 실사이트 | 명시적으로 허용된 조회→운임→필수 검증 | 준비 포함 주문0회, 실제 목표/계정/잔액/세션 검증. 준비 캡처가 주문을 만들면 이 범위에 포함 불가 |
| T2b 일반석 전체 | 달력 준비→사용자 시험 시작→API 주문1회→같은 주문 인계→실제 Npay | D1~D5 기능 연결, 준비 포함 실제 주문 건수 기록, 인계 후 재주문0회, 최종 승인 없음 |
| T2c 최종 후보 | 회사 Windows 동일 후보로 전체 흐름 연속2회, 세션 재준비 포함 | 실행/코드 해시 일치, 실패 포함 개별 성능, 이전 시험 주문 상태 확인. 조건/코드 변경 시 영향 구간 재검증 |
| T3 09시 실전 | 준비 조건·미검증·속도를 보고한 뒤 사용자가 적용·대기 결정 | 실제 신규 개방 여부와 결과 기록. 직전 미검증 변경/즉석 경로 교체 없음. T2 성공을 경쟁 성공으로 표현하지 않음 |

실사이트 요청·주문은 해당 시험이 명시적으로 허용된 범위에서만 한다. 일반석 반복 시험 전에 이전 주문 상태를 확인하며, 해제/복구 확인이 불가능하면 이를 숨기지 않고 후속 실제 주문 시험을 보류해 사용자에게 상황을 보고한다. 영구적으로 보유 시작 시각을 못 알아도 응답 확인과 정확한 시작 시각을 분리해 평가할 수 있다.

- 미개방 재조회는 조회만 허용된 횟수/간격/종료시각 안에서 수행한다. 밀린 요청 몰아 보내기 금지. 매진은 중단하고 다른 등급/날짜로 대체하지 않는다.
- 401/403·추가 인증·대기열은 정상 접근 절차로 처리, 429는 서버 대기 지침 준수. 오류를 재고 없음으로 분류하지 않는다.
- ICN 출발 성공은 현재 주문에 연결된 새 `pay.naver.com`/`m.pay.naver.com`의 실제 Npay 화면이다. 로그인·카드 PG·기존 창은 제외한다. 자세한 판정은 [상위 README](../README.md#validation).
- API 후반 단계가 실패하면 실패로 종료하고 **같은 주문 상태와 복구 필요**를 보고한다. 자동으로 UI 전체를 다시 돌려 주문을 만들지 않는다.

<a id="measurement"></a>
## 6. 성능·준비 완료 보고 계약

실행 ID·환경·목표·계정 익명 문맥·설정/실행 코드 해시와 다음 사건을 같은 실행에서 연결한다. 단조시계 경과시간과 절대시각 기준을 구분하며 Date 헤더를 재고 시각으로 사용하지 않는다.

| 지표 | 의미 |
|---|---|
| T0→조회 전송 | 시작 지연. 가상/예정/실제 개방 확인 구분 |
| 각 API 전송→첫 응답→본문→판정 | 실제 왕복·판정 비용. 순수 서버 처리라고 부르지 않음 |
| 응답 판정→다음 요청 | 제거할 수 있는 화면·로컬 대기와 필수 검증 비용 |
| T0→주문 전송 / pnr 응답 | 각각 기록. 실제 보유 시작 시각은 근거 없으면 미확인 |
| 인계 시작→동일 주문 확인→Npay | 후반 기능/비용. 주문 재전송·조회 횟수도 기록 |
| 실패·중단·준비 주문·해제 | 성공 표본만 남기지 않음. 해제 미확인은 그대로 표시 |

**준비 완료**는 목표·본인 계정/잔액·정상 세션·달력 시작 가능·검증된 전체 후보 코드·미해결 주문 처리·사용자 대기 조건을 확인한 범위로 보고한다. 캡처 complete나 과거120개 시험으로 대신하지 않는다. 정확한 서버 보유 시각/경쟁 성공은 별도 미확인일 수 있다.

<a id="handoff"></a>
## 7. 한 단계 종료와 기록

### 2026-09-14 주문 복구 점검 — 현재 우선 과제

**사용자 정정 반영:** 좌석 확보 단계는 예약 목록에 표시되지 않고 주문 완료 후 예약으로 표시된다. 따라서 예약 목록은 주문 완료 후 확인용이며 좌석 확보·해제 판정 수단이 아니다. 9/8·9/9 추가 예약은 사용자가 직접 만든 보너스 대기다. 아래 정상 목록 조사로 08:49 API의 좌석 상태를 판정하지 않는다. `unknown`은 로컬 응답 검증 실패 표지이며 서버에 좌석이 보유됐다는 증거가 아니다. 현재 우선 조사는 주문 응답의 의미와 금액 계약이다.

현재 Windows 실행 사본은 `flight_booking_astra_mac_review_20260914`이다. 운영 저장소와 상태 경로가 다르므로 양쪽을 혼동하지 않는다. 08:49 주문은 이 사본의 `dev-shots/state/order-intent-2026-09-14.json`에 `unknown / amount-mismatch`로 남아 있다.

**확인된 복구 결함:** `travellers.judge`는 금액 비교 실패 시 `pnr` 추출 전에 반환한다. `live_order`는 unknown을 기록하고 종료하며 `transport.SEND`는 응답을 브라우저에 보존하지 않는다. 따라서 현재 보존 자료만으로 그 주문의 예약번호·정확한 금액 차이를 복원할 수 없다. 9/14 추가한 금액 진단은 이후 실패의 조사 보완이며 기존 응답 복원·동일 주문 재개 기능이 아니다.

확인 순서는 다음과 같다.

1. 주문 완료 후 예약 확인이 필요한 경우에만 같은 회원의 정상 예약 목록을 조회한다. 좌석 확보 단계의 복구를 위해 목록 조회를 반복하지 않는다. 로그인 만료·로딩·조회 실패를 예약 없음으로 분류하지 않는다.
2. 목표 날짜·노선이 나타나면 정상 상세 조회로 날짜·편명·등급·상태를 대조한다. 기존 로마 예약 등 다른 주문은 변경하지 않는다. 단순 여정 일치는 오늘 실행의 주문 동일성 증거와 구분한다.
3. 동일 주문을 특정할 수 있을 때만 정상 재개 경로를 검토한다. 조사 중 새 주문 요청을 보내지 않고, 취소나 최종 결제는 하지 않는다.
4. 좌석 관련 상태는 해당 API 응답과 정상 서버 처리 계약으로 조사한다. 화면에 없음·시간 경과·Chrome 종료·재로그인은 해제 확인이 아니다. 로컬 검증 실패 상태와 서버 좌석 상태를 분리하고, 자동 재전송 여부를 예약 목록으로 결정하지 않는다.

**실패 응답 보존 구현(9/14, 로컬 검증):** `live_order.py --inspect-failure`를 지정한 실행은 주문 응답이 실패 판정되면 unknown·금액 진단을 기록한 뒤 실행 프로세스 메모리에 응답과 운임 문맥을 유지한다. 열린 표준입력이 필요하다. `summary`는 허용된 금액 차이·예약번호 존재 여부만 출력하고 `exit`·EOF·Ctrl+C는 조사 종료다. 조사 모드는 사이트 요청·결제·상태 해제 명령이 없다. 종료 후 원문 복원은 불가능하며 운영체제 메모리의 보안 삭제를 보장하지 않는다. 옵션을 생략하면 종전처럼 종료한다. 현재의 08:49 응답을 소급 복원하지 못한다.

다음 미구현 범위는 보존한 응답을 이용하는 동일 주문 읽기 전용 조사/재개 연결이다. 조사 모드 진입 자체를 동일 주문 복구 성공으로 보고하지 않는다. 원문·예약번호를 파일에 저장하지 않고 금액/통화 비교를 완화하지 않는다. 이미 남은 unknown·전송권은 그대로 유지한다. 전체 사이트 리허설 명령은 준비 검증 후 별도로 확정하며 이 옵션 추가만으로 재전송을 허용하지 않는다.

### 금액·화면 연결 조사 결과 (9/14, 저장 소스 정적 분석)

#### 저장 모델 연결 후보 (9/14 추가)

9/14 14:21 실사이트 시도: 사용자 반복 진행 지시를 별도 일반석 시험1회로 기록해 `explicit_retry.py`를 추가했다. 정확히 일치하는 이전 unknown runId·동일 날짜/목표/일반석만 허용한다. 원래 intent/permit을 retry-history에 보존하고, 기존 permit을 삭제하지 않으며 실제 전송 직전에 새 전송권으로 교체한다. 자동 반복 호출은 없고 같은 이전 runId claim 중복은 거부한다. 이 승인은 서버 해제 확인이 아니다. 관련53개 T1 통과 후 실제 실행했다.

실측: 14:21:26 준비 시작, 14:22:06 승객/연락처 및 캡처 완료(주문 seen1/blocked1/unblocked0), 14:22:16 달력35셀 복귀, 14:22:17 발사 전 준비 검증 실패. fire와 handoff는 빈 장부, 신규 주문0건. 예외를 별도 읽기 전용으로 좁혀 search-target-mismatch 확인. 실측 검색은 originLocationCode=sel, originLocationAirportType=CTY, originLocationCountryCode=KR, 날짜2027-09-07T00:00:00.000, 목적지CDG였다. sel을 실제ICN과 같다고 비교한 것이 아니라 검색 범위와 실제 공항의 코드 차이였다.

수정: 이 관측된 도시 검색 조건은 허용하되 실제 운임/주문 출발 공항은 반드시ICN으로 기존 검증한다. GMP 주문/CTY 아닌 sel은 거부하는 반례 포함 관련19개 시험 통과. 수정 후 실사이트 재시도는 미실행이며 금액 오류 해결/실제Npay 도착을 보고하지 않는다. 현재 전송권은 아침 runId 그대로, 재시험 claim은 소비됨. 다음 재개는 준비 실패로 실제 주문0건이었다는 실행 근거를 보존하는 절차가 필요하다. 단순 claim 삭제나 원래 unknown을 resolved로 바꾸지 않는다.

**최신 실행기 연결 상태:** `live_order.py --state-bridge --continue-payment --inspect-failure` 분기를 추가했다. 같은 날짜·일반석 즉시 리허설에만 허용하며, 다른 캡처 날짜·프레스티지·예약시각(--at)·gate-only 혼용은 준비 전 거부한다. 연구 경로는 정확한 달력 탭이 하나일 때만 시작한다. 발사 전에 문서/회원 저장값/쿠키 결속과 저장 모델의 목표 여정·기존 주문·준비 상태를 확인한다. 기존 예매/계측 및 기본 실행 분기는 교체하지 않았다.

검증된 주문 응답 이후 `bridged_payment`가 connect→같은 주문 참조/날짜·마일리지·KRW 문구/세션 변화 없음 확인→기존 `payment_pass(navigate=False, existing_watch=...)`를 호출한다. 후반 함수는 전달된 감시가 실제 대상 페이지/컨텍스트의 활성 감시인지 다시 확인한다. 게이트를 다시 열지 않고 기존 동의/마일리지/Npay 판정을 수행한다. 실패 시 메모리 응답 조사로 전환하고 조사 종료까지 감시를 유지한 뒤 명시 종료한다. 프로세스 종료 뒤까지 네트워크 차단이 남는다고 보장하지 않는다.

검증: 실행기·기존 결제 분기 관련83개 로컬 시험 통과. 새 분기의 가짜 전송은 주문1회, 격리 후반 시험은 게이트 이동1회·Npay 창1개·최종 승인0회를 확인했다. 합성 응답/앱 시험이므로 실사이트 결제 성공은 아니다. 준비 저장 상태 확인을 발사 전으로 앞당긴 후 영향 시험을 추가했다. 금액 불일치 검사를 제거하지 않았고 08:49 응답을 복원하지 않았다. 현재 unknown 표지와 전송권은 보존 중이며 신규 실사이트 리허설은 미실행이다. 별도 교차 검토도 미완료다.

아래 '실행기 미연결' 기록들은 위 연결 이전 단계의 조사 이력이다. 현재 완료 범위는 연구 분기 연결과 T1이며 T2/T3 성공이 아니다.

최종 영향 시험: 준비 저장값의 발사 전 검증을 추가한 뒤 실행기 연결/저장 모델/통합 연결 22개 통과. 기존83개와 시험 범위가 겹치므로 고유 시험105개로 합산하지 않는다.

통합 연결 함수 추가: `connected_bridge.bind`는 발사 전 정확한 달력 탭에 문서 표지를 만들고 쿠키 값·회원 저장값·네 상태 모델의 문맥을 메모리에 보관한다. `connect`는 같은 탭/문서·로컬 실행 session/subject·유효 시간·쿠키/회원 저장값이 유지되고 기존 금액/여정 검증을 통과한 응답에 한해 저장 모델 적용→결제 게이트 이동→동일 주문 참조 감시를 수행한다. 로그인 상태를 새로 만들거나 동의/결제 버튼을 누르지 않는다. 이 모듈은 실제 사이트 오리진으로 작성됐으나 이번에는 네트워크 격리된 합성 앱에만 적용했고 live_order에는 아직 미연결이다.

반환 `Connection`은 감시를 유지하며 호출자가 명시적으로 `close()`해야 해제된다. 이동/관측 예외에도 감시를 유지하고 민감 정보가 섞일 수 있는 예외 원문은 출력하지 않는다. `result.matched`는 주문 참조 일치 의미다. 날짜/마일리지/KRW 문구는 `displayHints`, 쿠키/회원 저장값 변화는 `sessionUnchanged`로 별도 반환한다. 전체 화면·최종 결제창 성공과 동일시하지 않는다. 쿠키/회원 저장값 불변은 서버 인증 유효성 증명이 아니고 정상 쿠키 갱신도 보수적으로 거부할 수 있다.

통합·감시 16개 로컬 시험 통과: 정상 모델 적용/이동/참조 확인, 후발 주문 차단, 쿠키 변경, 같은 URL의 문서 교체, 저장 모델 변경, 다른 주문 참조, 초기화 중 재주문, 이동 예외, 잘못된 응답 등을 검증했다. 초기 시험에서 HTML 한글 인코딩과 request 이벤트가 route.abort 완료보다 먼저 도착하는 문제를 발견해 수정했다. 차단 결과 미확정 시 제한 시간 내 다시 판정하며 실제 미차단을 성공으로 허용하지 않는다. 실제 Angular 결제 앱을 쓴 시험은 아니다. 별도 교차 검토는 사용량 제한으로 미완료다.

남은 실사이트 연결 조건: prepare/run에서 API 전송 전에 Binding 생성, 실제 응답을 동일 Pipeline과 함께 connect로 전달, 같은 주문의 화면 전체/금액 확인 후 기존 동의·Npay 단계 연결, 실패 응답 조사 수명과 guard 수명 통합. 현재 날짜가 다른 캡처는 거부하므로 신규 개방 적용은 별도 과제다. 실사이트 데이터로 이 단계들을 검증하기 전 전체 준비 완료로 보고하지 않는다.

인계 감시 후속: `bridge_guard.py`는 기존 HandoffWatch를 확장해 주문 inputTravellers와 항공편/운임 선택 API를 모두 차단한다. 새 문서의 baggagePolicies 주문 참조를 대조하고, 참조 일치 후에도 감시를 유지한다. 후발 주문 시도는 차단 후 실패 판정, 다른 주문/다른 탭/잘못된 URL/참조 미관측/페이지 종료는 성공이 아니다. stop 후에는 판정을 거부한다. 이 모듈 자체는 이동·상태 쓰기·결제를 하지 않으며 live_order에 아직 연결하지 않았다. 실제 UI 여정/금액 검증 및 회원 세션 결속도 별도다.

격리 시험: guard 최초7개+저장 모델9개+실제 복원 라이브러리2개 = 18개 통과. 이후 stop 후 판정 거부 시험을 추가했다. 가짜 결제 앱의 요청을 검증하는 것이며 실제 대한항공 결제 앱 렌더링 시험이 아니다. 사이트·주문·동의·결제 조작 없음. 별도 교차 검토는 앞선 도구 사용량 제한으로 미완료이며, 운영 연결 전에 보완한다.

후속 복원 라이브러리 실행 검증: `test_site_rehydrate.py`는 해시 고정된 9/10 사이트 JS의 실제 모듈42584를 격리 Chromium에서 실행한다. 검증 후보가 만든 합성 운임/주문 저장 모델이 초기 상태로 복원되는 시험과, 복원기가 생성될 때 저장값을 읽는 시험 2개 통과. 후자는 기존 앱에서 sessionStorage만 수정하면 자동 갱신되지 않는다는 연결 제약의 근거다. 전체 Angular reducer/결제 앱 렌더링·네트워크 흐름은 실행하지 않았다. reducer는 시험용 함수이며 라이브러리 자체는 원본이다.

이 신규 시험 파일의 별도 교차 검토는 검토 도구 사용량 제한으로 실패했다. 로컬 시험 2개 통과와 구분하며 교차 검토 완료로 보고하지 않는다.

현재 배포본 대조(9232 읽기 전용 CDP, 추가 사이트 요청/주문 없음): `/5064.b9839b5abd2daa1c.js` SHA256 `19e2ba034b1b503e253d8ccfbe42c391f2b4b369019199b9fbc86e045fbd86dc`의 42584 복원 모듈 및 82484 주문 reducer 소스 구간이 9/10 저장본과 동일했다. `/5856.f307f0ab6fc54a55.js`에서도 fareInformation/inputTravellers 키와 sessionStorage rehydrate 설정을 확인했다. 전체 파일 동일성·다른 모듈 호환성까지 확인한 것은 아니다.

운영 연결에 남은 조건: 현재 문서·회원 세션과 검증 응답을 결속하고, 상태 반영 후 앱을 새로 구성할 때 선택/주문 재전송을 차단하며, 실제 화면의 주문 참조·여정·금액을 확인해야 한다. 현재 후보는 캡처와 목표 날짜가 같은 경우만 허용하므로 09시 신규 목표로의 적용을 완료했다고 보고하지 않는다. 이번 단계는 브라우저 상태 쓰기나 실제 예약/결제 조작을 하지 않았다.

- 근거 추가: `5856.4d8028464152ae94.js`는 `fareInformation`·`inputTravellers` 등을 sessionStorage에서 rehydrate한다(SHA256 `267bbe4828fb96d936d85da2fd0871e9afe8b1db78ee85253620273d325b70bb`). `5064.f719afa8cfe71e62.js`는 두 reducer의 `{model,errors,isPending,isFailure}` 구조를 정의한다(SHA256 `96643181f464c3a67439ade352d0cdd38d10ea3c4719176fa5bd358457beefd7`). 현재 9232에서 읽기 전용으로 같은 저장 키/구조를 확인했다. 로그인/동의 통과나 현 배포 전체 소스 일치를 확인한 것은 아니다.
- `state_bridge.build_patch`는 기존 운임/주문 검증을 다시 적용한 뒤 두 저장 모델만 만든다. 준비 검색의 날짜·노선이 목표와 다르면 거부한다. 기존 주문 모델·진행 중 상태·동의 상태가 있으면 덮어쓰지 않는다. `resvStatus`와 검색 조건은 변경하지 않는다.
- `fixture_apply`는 `https://bridge.invalid` 전용이며 실사이트 도메인을 거부한다. 적용 직전 네 저장값의 일치 여부를 비교하고 변경됐으면 거부한다. 따라서 운영 실행기에는 연결하지 않았으며, 이 후보로 실사이트 주문을 이어갈 수 있다고 보고하지 않는다.
- 격리 Chromium에서 저장 모델 쓰기→재로딩→동일 합성 응답 읽기와 API 요청 0건을 확인했다. 실제 Angular 앱을 로드한 시험이 아니므로 앱 재수화·추가 요청·Npay 성공 증거가 아니다. 서로 다른 캡처/목표 날짜, 실제 회원 세션 결속, 페이지 이동 중 재주문 차단, 같은 주문 참조 확인을 해결해야 운영 연결이 가능하다.
- 시험: 관련 묶음 98개 중 1개가 기존 `permit.durable_json`의 임시파일 `os.replace`에서 Windows 접근 거부(WinError 5)로 실패. 원인 미확정이며 환경 탓으로 확정하지 않는다. 해당 시험과 신규 후보 8개를 재실행한 9개는 통과했으나 최초 실패를 삭제하지 않는다. 실사이트 실행 기록 저장의 신뢰성 과제로 별도 추적한다.
- 읽기 전용 교차 검토 후 개인정보 설명과 시험 이름을 실제 범위에 맞게 수정하고, 두 번째 저장 실패 시 두 모델이 원복되는 시험을 추가했다. 최종 신규 후보 시험은 9개 통과. 저장소 생존 시험이지 실제 앱 재수화 시험이 아니며, sessionStorage는 브라우저가 관리하는 저장소이므로 순수 프로세스 메모리 보존과 구분한다.

실패 응답 보존 후속 검증: 판정 코드 자체의 Exception도 unknown 기록 후 보존 모드로 연결했다. 관련 로컬 90개 시험 통과(판정 실패·단일 주문 전송·재시작 차단·EOF·조사 중 Ctrl+C·예외 문자열 비노출). 네트워크 전송 중 응답 자체를 받지 못한 경우나 프로세스 강제 종료는 복원 대상이 아니다. 별도 읽기 전용 교차 검토 지적 1건을 수정했다. Git 없는 사본이므로 ke-review 래퍼 검토는 실행되지 않았다.

근거는 운영 폴더의 `dev-shots/api-source-2026-09-10/4027.091b94da2bfc20d6.js`다. SHA256 `3a2134d0ba92bab721b21e9cfd393222ebe558325a9fa430af885942f35f7653`. 9/10 저장본이며 현재 배포본과 일치 여부·실사이트 재현은 아직 검증하지 않았다.

- 문자열 위치 3486757 부근: 정상 화면은 `awardInputTravellersState.model`을 `prebookReply`로 사용하고, 여기서 `pnr`·`officeId`·`boundList`·`pnrFareInfo`를 가져와 결제 화면을 구성한다. 운임 단계에서는 `awardFareInformationState.model`을 사용한다.
- `setPaymentInterface`의 NR 분기는 `pnrFareInfo.totalAmount`를 결제금액으로, `pnrFareInfo.mileage`를 마일리지로 사용한다. 조사한 직접 접근 `pnrFareInfo.amount`는 발견하지 못했다. 이것만으로 amount가 무의미하거나 두 응답 간 달라도 된다는 결론은 내리지 않는다.
- 현재 검증기는 운임/주문 amount·totalAmount·mileage의 동시 일치를 요구한다. 결제용 totalAmount·mileage의 의미는 확인했지만, amount까지 동일해야 하는 계약은 아직 입증되지 않았다. 08:49 실제 불일치 필드는 응답 미보존으로 미확인이다. 14:32 별도 시험은 amount만 0→349500.00 불일치했고 totalAmount 349500원·mileage 35000·통화는 일치했다. 최신 정책은 아래 보완을 따른다.
- 현재 `transport.SEND`는 원본 fetch 응답을 Python으로 반환할 뿐 사이트의 위 상태를 갱신하지 않는다. 따라서 API 성공 후 단순 결제 URL 이동만으로 정상 화면과 같은 상태가 된다고 볼 수 없다. 정상 상태 반영 또는 검증된 같은 주문 재개 경로가 필요하다.
- 사이트의 `isPNRCreated`·`isPNRConfirmed`는 클라이언트 상태 플래그다. 서버 좌석 확보 시각이나 최종 발권 완료의 직접 증거로 사용하지 않는다. 결제 이후 `issueTicketAfterPayment` 호출 경로도 별도로 존재한다(이번 조사에서는 호출하지 않음).

다음 증거 수집: 새로 승인된 일반석 시험에서 현재 코드의 검증을 유지한 채 운임/주문 수치 차이를 남기고, 응답과 주문 참조는 실행 메모리에서 유지한다. 차이가 amount만인지, 최종 합계나 필요 마일리지도 바뀌는지 확인한 후 비교 정책을 결정한다. 예상값을 주문 응답으로 무조건 덮어쓰거나 금액 검사를 제거하지 않는다. 동일 주문 화면 연결은 별도 기능 검증 대상이다.

9/14 오후 재확인: 9232를 동일 운영 프로필로 다시 열었으며 최초에는 로그아웃 상태였다. 사용자 로그인 완료 후 `/reservation/list`의 로딩 종료를 확인했다. 목록에는 기존 8/28·9/4 로마 왕복과 9/8 1건·9/9 2건의 인천→파리 여정이 보였고 9/7은 없었다. 9/8·9/9는 아침 API 요청의 목표 날짜와 다르므로 그 주문의 복구 대상으로 취급하지 않는다. 각 예약의 확보·발권 상태나 생성 주체는 이번 목록 조사로 확정하지 않았다. 새 주문·취소·상태 해제는 실행하지 않았다. 기존 9/7 주문을 특정할 참조가 없어 재개·해제 확인은 미완료다.

- NOW에는 완료 범위·현재 첫 작업 하나만 갱신한다. 이 문서 §4에는 단계 상태/계약을 반영한다.
- 길어지는 실행별 기록은 `docs/archive/<날짜>/`에 남긴다. 구현/시험 파일·실행 명령·환경·종료 코드·통과/실패·해시·미확인·정확한 다음 작업을 포함한다.
- 관련 로컬 시험은 `python api_booking/test_<대상>.py`를 개별 실행한다. Windows는 `.\.venv\Scripts\python.exe`, Mac은 `./.venv/bin/python`. 실제 존재하는 시험명을 선택한다.
- 실행 코드 변경의 교차 검토는 [ke-review](../.claude/skills/ke-review/SKILL.md)를 따른다. 정적 검토를 실사이트 검증으로 세지 않는다.
- 문서화 여유를 남겨 한 단계를 시험 가능한 상태로 마감한다. 담당 전환 시점은 사용자가 결정한다.

<a id="claude-prompt"></a>
## 8. 클로드에게 붙여 넣을 시작 프롬프트

```text
현재 flight_booking_astra 작업 폴더에서 API 예매 개발을 이어가줘.
AGENTS.md → NOW.md → README.md → FACTS.md → api_booking/README.md를 읽어.
개발 원본은 api_booking/README.md §3~§6이며 과거 A1~A8 기록의 다음 작업은 따르지 마.
목표는 달력에서 준비·시작 → 조회·운임·필수 검증·주문 API → 같은 주문 브라우저 인계 → 동의·마일리지·실제 Npay야.
어제 주문 페이지의 캡처에서만 발사되는 구조나 게이트 도착을 전체 완료로 보고하지 마.
이번에는 §4의 D1 동일 주문 인계 계약 확인과 최소 로컬 검증 하나만 진행해.
기존 코드·보존 소스·9/13 검토를 대조하고, API pnr과 실제 화면 주문의 연결 경로 및 페이지 전환 부작용을 확인해.
날짜·마일리지가 같아도 이전 주문이면 거부하고, 인계 중 새 주문을 만들지 않는 반례 시험을 포함해.
계약 근거가 없으면 합성을 실증으로 바꾸지 말고 부족한 증거와 다음 수집 항목 하나를 특정해.
기존 A1~A6를 다시 만들거나 먼저 잠금 기능부터 확장하지 마. 후속 순서는 D2→D3→D4→D5야.
회사 Windows 호환을 유지하고, 단계별 관련 로컬 시험을 실행해.
이번 요청은 코드·문서·로컬 시험 범위야. 실사이트 요청·로그인 조작·주문·결제창 시험은 별도 허용 전 수행하지 마.
캡처 자체가 주문을 만들 수 있고 --dry도 준비까지 무주문을 보장하지 않아.
운영 포트·계측·시계·일정·실전 대기를 자동 변경하지 말고 미커밋 코드와 로그를 보존해.
종료 전에 변경 파일·명령·결과·미확인·다음 작업 하나를 문서에 남기고, 이번 완료/변경/시험/미확인/다음 작업 순서로 보고해.
```


## 2026-09-14 금액 판정 보완 — 현재 연구 구현

실측·시각·범위는 과거 [14:32 기록](../docs/archive/2026-09-14/historical-api/rehearsal-2026-09-14-1432.md)과 최신 [16:37 기록](rehearsal-2026-09-14-1637.md)을 구분한다.

- 기본 검증기는 기존의 전체 금액 일치를 유지한다. live_order의 `--state-bridge`만 관측 형태 정책을 명시적으로 전달한다.
- 예외는 ICN→CDG / KE901 / KEBONUSEY / KRW, 운임 amount=0, 주문 amount=동일 totalAmount, 총액과 마일리지 정확 일치 및 양수인 경우뿐이다. 타 노선·프레스티지로 확장하지 않는다.
- 응답값을 바꾸지 않는다. `amountsMatched=False`, `paymentAmountsMatched=True`, `amountLayout=observed-zero-to-total`로 기록하며 화면 인계 성공/실패/예외 뒤에도 이 근거를 보존한다.
- 여정·회원 문맥·참조·HK·동의·결제 제공자 검증은 그대로다. HK/주문 참조를 좌석 확보 시각으로 해석하지 않는다.
- recovery의 선택적 resume 콜백은 재주문 없이 원래 응답을 재검증하고 인계를 1회 시도하는 기반이다. 라이브 helper는 기존 30초 인계 유효시간과 binding 미사용을 확인한다. 최초 판정과 같은 정책이므로 결정론적인 검증 실패를 해결하지 않는다. 프로세스간 복원이나 실행 코드 갱신 기능이 아니다.
- 실행 중인 구버전60979는 summary/exit만 지원하고 이미 유효시간을 넘겼다. 이번 주문을 새 코드로 이어갈 수 있다고 보고하지 않는다. unknown/permit을 보존하며 다음 실사이트 시험과 구분한다.
- T1 관련119개 통과, 추가 복구/인계 기록 회귀 통과. 읽기 전용 교차 검토에서 인계 기록의 금액 근거 누락1건을 채택·수정했다. 수정 후 Npay 도착은 미검증이다.


### 9/14 쿠키 회전과 문맥 비교 보완

`connected_bridge._session_stamp`는 실제 달력 무조작15초 동안 값 회전을 관측한 bm_s/bm_sv만 값 비교에서 제외한다. 존재 여부·이름·도메인·경로와 다른 쿠키 값 및 회원 저장값은 계속 비교한다. 쿠키 원본/사이트 요청은 변경하지 않는다. 이는 클라이언트 문맥 검사이지 서버 로그인 유효성 증명이 아니다. 과거 실패의 직접 원인은 당시 기록으로 보존하며, 최신 성공은 [16:37 기록](rehearsal-2026-09-14-1637.md)을 따른다.


### 주문 직전 인계 사전 검사

연구 state-bridge는 조회/운임/주문 준비 후 `connected_bridge.preflight`로 문서·세션·저장값·목표·시간을 읽기 대조한다. 불일치하면 전송권 취득 전에 종료한다. 검사는 원래 binding을 유지하며 새 기준으로 교체하지 않는다. 이 검사로 이후 서버 주문 응답·게이트/Npay 성공을 보장하지 않고, 주문 후 검사도 그대로 수행한다. 관련67개 T1/읽기 교차검토 통과. 실제 달력 현재상태의 독립 점검은20.97ms였으며 신규개방 실전 지연의 보장값이 아니다.


### 보존 응답의 읽기 진단과 조건부 인계 재개

수정본의 화면 인계 실패/예외는 `inspect_handoff`를 통해 원래 응답·운임·문서 binding·주문 문맥을 조사 콜백에 유지한다.

- `summary`: 금액/마일리지만 허용목록으로 출력.
- `diagnose`: 현재 문서/회원 문맥/저장값 일치, binding 사용 여부, 경과 시간, 30초 이내 여부를 읽어 표시. 시간이 지난 뒤에도 조사는 가능하다. 기준 시각이나 쿠키·저장값은 변경하지 않는다. 출력은 고정 bool/유한 숫자로 제한한다.
- `resume`: binding 미사용인 경우만 제공. 같은 요청/응답 객체를 다시 검증하고 기존30초 범위 안에서 인계1회. 이미 모델을 적용한 binding으로 다시 적용하거나 주문/조회로 돌아가지 않는다. 실패하면 원래 조사로 복귀하고 중첩 조사창을 만들지 않는다.
- `exit`/EOF: 메모리 보존 종료. 프로세스 재시작 뒤 복원하거나 수정 코드를 실행 중 프로세스에 반영하는 기능은 없다. 결정론적인 응답 검증 오류를 코드 변경 없이 해결하는 기능도 아니다.

관련76개 T1 통과, 읽기 전용 교차검토에서 구체 결함 지적 없음. 현재65637은 수정 이전 실행으로 이 명령들이 소급 연결되지 않는다. 기존 주문 복구/Npay 도착/프로세스간 복원은 미완료이며 새 주문은 보내지 않았다.


## 사용자 승인 주문 증거 저장 — 2026-09-14

사용자 명시 허용 범위는 AGENTS §5를 따른다. 구현은 `order_evidence.py`, 실제 실행 연결은 live_order의 주문 응답 수신 직후이다. 판정 전에 저장하므로 amount-mismatch나 판정 코드 예외가 나도 수신 증거가 남는다.

경로: `dev-shots/state/order-evidence/<runId>/received.json` 및 `judgment.json`. dev-shots는 .gitignore 대상이다. 식별자 원문은 이 로컬 자료에만 저장하고 터미널/일반 문서로 출력하지 않는다.

| 저장 항목 | 출처·한계 |
|---|---|
| pnr, orderId | 주문 응답 최상위의 같은 이름 필드만. 미관측/형식 불일치=null, 다른 값을 추측 대입하지 않음 |
| 목표 날짜·출발·도착·항공편·운임 등급 | 이번 실행 Target. 응답 여정 자체를 대체하는 증거가 아님 |
| segmentStatus | 응답의 단일 bound/segment 상태. 좌석 확보 확정 의미를 부여하지 않음 |
| currency, totalAmount, mileage | 응답 pnrFareInfo의 해당 값 |
| serverOrderCreatedAt | 서버 생성 시각 필드 계약 미확인으로 null 유지 |
| orderRequestStartedAt | 브라우저 fetch가 기록한 startedAt를 UTC로 변환. 서버 생성 시각 아님 |
| orderResponseReceivedAt | Python이 반환 응답을 받은 직후 시각. 저장 전에 고정 |
| 실행 ID·응답 진단 | 고정 진단값/정제한 숫자·불리언. 임의 오류 원문 제외 |
| passengerFingerprint | 이미 검증한 travellerId 목록의 SHA-256. 이름/연락처/식별자 원문 제외. 익명화 보장은 아님 |

응답 전체, headers, 쿠키, 인증/결제 토큰, pageTicket, 승객/연락처 원문은 저장하지 않는다. 저장 실패는 별도 로그로 알리고 재주문하지 않는다. 원래 수신 파일은 판정 파일과 분리하여 덮어쓰지 않는다. 이 저장 함수는 자동 재예매·결제·전송권 해제를 수행하지 않는다.

현재 구버전65637에는 새 저장 함수가 연결되지 않는다. 기존18e1ea6cd29c는 확인 가능한 기존 로그만 `historical-summary.json`에 별도 저장했다. pnr/orderId/승객 지문/서버 생성시각/정확한 요청시각은 복원하지 못해 null이며, 수신시각은 당시 로컬 응답 로그에서 가져온 것으로 표시했다. 이를 원래 전체 응답으로 취급하지 않는다.

검증: 관련61개 T1 통과. 금액 판정 예외 전 식별자 저장, 비허용 원문 제거, 경로 이탈 거부, 수신기록 보존, 저장 지연을 응답시간에서 제외하는 회귀 포함. 최초 시험의 경로 검사 오류 수정 후 통과했으며 기존 permit 원자 교체에서 WinError5도1회 발생했다. 재시험은 통과했지만 Windows 파일 교체의 간헐적 실패 원인은 미확정이다. 교차 검토 지적1건(파일저장 지연이 응답시간에 혼입)은 채택·수정했다.

이 항목들로 프로세스 종료 후 주문 식별·실패 비교는 가능해진다. 사이트 상태모델 전체 복원이나 재조회 API 연결은 아직 없으며, 이를 위해 추가 자료가 필요하면 사용자에게 정확한 필드를 제시해 검토받는다. 이번 작업은 새 실사이트 주문/결제를 실행하지 않았다.


## 2026-09-14 최신: 기존 게이트의 같은 주문 복구

이 절은 과거 복구 상태를 보존한다. 최신 연속 성공 runId는 b566b85f58a4이며 [16:37 보고서](rehearsal-2026-09-14-1637.md)를 따른다.

`resume_gate.py --run-id <허용 저장 실행 ID> --port 9232`는 이미 모델이 적용된 결제 게이트가 정확히 하나 있을 때만 사용한다. 수신 증거와 현재 PNR·여정·HK·KRW총액·마일리지·승객 지문·로그인 상태를 대조하며, 결제하기 전후에도 현재 회원과 모델을 재확인한다. 신규 주문/선택 요청은 guard가 차단한다. 네트워크 참조를 관측하지 못한 기존 문서는 same-order-stored-model로 구분하며 과거 요청을 관측했다고 꾸미지 않는다. Npay 자체 요청의 참조 검증은 별도로 유지한다.

필수 동의 처리 후 마일리지가 이미 적용된 경우 정확한 숫자와 완료 표시를 읽는다. 적용되지 않은 상태는 실패로 종료하며 자동 재적용하지 않는다. 같은 주문 Npay까지 열 수 있지만 최종 결제는 하지 않는다. 실제 실행에는 현재 사용자 시험 범위와 창 상태를 먼저 확인한다. 이미 성공한 결제창을 다시 확인하려고 복구 명령을 반복하지 않는다.

수신 증거만 있고 게이트 모델이 사라졌다면 이 경로로 복원할 수 없다. 서버 생성 시각은 여전히 계약 미확정으로 null이다. 운영 자동 예매에 이 사본을 병합하지 않았으며 후속 주 실행 통합·일반석 연속 검증이 남아 있다.


## 2026-09-14 주 실행 통합 상태(최신)

[16:37 리허설](rehearsal-2026-09-14-1637.md)이 현재 기준이다. connect는 준비 회원 지문과 요청/응답/모델을 비교하고, 새 문서 참조 관측 및 최대10초 화면표시 대기를 유지한다. 이동 후 쿠키 변화는 진단값이며 회원·로그인 상태·주문 모델의 일치를 대체하지 않는다. 주 실행과 기존 게이트 복구 모두 ensure_mileage로 정상 적용 버튼1회·UI 완료·정확한 마일리지를 확인한다.

최신 실제 주문 fd50ce2a123a는 API→브라우저 후 중단, 동일 주문 후반 자동 복구로 Npay 도착했다. 달력부터 중간 개입 없는 완주는 남아 있다. 과거 Npay 시험 성공 뒤 별도 일반석 재시험을 승인한 경우에만 --retry-after-checkout와 --retry-of를 사용한다. 기존 성공/전송권을 보존하며 해제 확인으로 바꾸지 않고, 일반 실행·자동재시도에는 쓰지 않는다.

9232 연구 프로세스가 입력 대기 중이면 신규 감시 설치가 멈춘 사례가 있다. 작업 시작 전 기존 연구 연결 실행을 확인하고 정상 종료한다. browser_identity 표시 작업과 다른 프로필은 종료 대상이 아니다. 현재 이전 조사 프로세스는 종료됐으며 성공 결제창 보존을 위해 조사 프로세스를 계속 남기지 않는다.
