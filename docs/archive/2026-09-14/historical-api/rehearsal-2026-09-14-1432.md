# 2026-09-14 14:32 API 일반석 리허설

## 결과와 현재 상태

- 사용자 지시: 달력을 ICN으로 수정한 뒤 2027-09-07 ICN→CDG KE901 일반석 재개. Chrome 9232, Windows, KST. 이미 열린 날짜의 T2 시험이다.
- 실행 ID `24e42d5bbad9`. 새 실제 주문 요청 1회, HTTP 200. `amount-mismatch`로 중단, Npay 미도착. 좌석 확보 여부·시점 미확인.
- 총액과 마일리지는 일치했다. 아래 `amount` 동일성 가정 때문에 로컬 검증이 실패했다. 이것을 서버 주문 실패나 좌석 미확보로 해석하지 않는다.
- 프로세스는 조사 입력 상태로 유지했다. 이 작업의 실행 세션 ID는 `60979`. `summary`는 허용된 금액만 출력한다. `exit`/EOF/프로세스 종료는 메모리의 응답을 잃게 한다. 현재 조사 명령에는 같은 응답으로 인계를 재개하는 기능이 없다.
- 새 주문 재전송·취소·최종 결제 없음. 기존 아침 시도는 감사 이력에 보존했고 서버 해제 확인으로 변경하지 않았다.

## 같은 실행의 실측

| 단계 | KST 시각 또는 소요 | 의미 |
|---|---|---|
| 준비 시작 | 14:31:15.771 | 날짜 선택→운임→승객/연락처 초안 |
| 준비 완료 | 14:31:57.752 | 준비 주문 요청 1회 차단, 미차단 0회 |
| 달력 복귀 | 14:32:08.154 | 35개 셀 확인 |
| T0 | 14:32:08.508 | 즉시 리허설 시작, 09시 개방 시각 아님 |
| 항공편 조회 완료 | T0+1.518초 | HTTP 200, 요청 1.510초 |
| 운임 조회 완료 | T0+2.923초 | HTTP 200, 요청 1.398초 |
| 주문 준비 완료 | T0+2.924초 | 필수 로컬 검증 통과 |
| 전송 의도 기록 | 14:32:11.446915 | T0+약 2.94초, 실제 전송 직전의 대리 지표 |
| 주문 응답 완료 | 14:32:17.478 | T0+8.969초, 요청 6.014초 |

응답 완료 시간을 좌석 확보 시간으로 볼 수 없다. 준비 약 52.7초는 T0 이전이며, 기존 승객 초안 준비를 새 속도 개선 성과로 계산하지 않는다.

| 필드 | 운임 응답 | 주문 응답 | 비교 |
|---|---:|---:|---|
| amount | 0 | 349500.00 | 불일치 |
| totalAmount | 349500 | 349500.00 | 일치 |
| mileage | 35000 | 35000 | 일치 |

통화도 일치했고 주문 참조 필드가 있었다. 참조 원문은 문서·로그에 기록하지 않는다. 이 결과만으로 아침 08:49 실패의 세부 값도 같았다고 확정할 수 없다.

## 이번 수정과 검증

- 실측 저장 날짜 `2027-09-06T15:00:00.000Z`를 Asia/Seoul 달력의 2027-09-07로 처리했다. 브라우저 timezone과 로컬 날짜를 직접 대조했다. 현재 승인된 KST 실행 범위용이며 다른 시간대 적용은 별도 검증 필요.
- 주문 전 중단됐던 준비의 사용자 지시 재개를 `--resume-preparation --retry-of`로 1회 허용했다. 이전 permit 일치, 미전송 준비 상태, 기존 감사 기록 일치를 검사하고 재개 기록을 별도 보존한다. 실제 주문 뒤에는 같은 경로 재사용 불가.
- `test_explicit_retry test_state_bridge test_bridge_runner test_live_order`: 67개 통과. 실사이트 준비 저장값 사전 검증도 통과했다.
- 운영 예매와 계측기 코드는 변경하지 않았다.

## 다음 작업

1. `amount`의 단계별 의미를 정상 브라우저 응답·사이트 코드와 대조한다. 총액 일치만 보고 검증을 무조건 제거하지 않는다.
2. 이미 받은 응답을 재주문 없이 재검증·인계하는 복구 경로가 필요하다. 현재 실행의 조사 모드는 summary/exit만 지원하므로 코드 수정만으로 실행 중 프로세스가 자동 갱신되지 않는다.
3. 같은 주문 상태 복원, 동의, 실제 Npay 도착을 확인해야 최종 기능 완료다. 현재는 미완료다.

상태 근거: `dev-shots/state/order-intent-2026-09-14.json`, `order-permit.json`. 아침 원래 기록: `dev-shots/state/retry-history/9ddc49eb8a31/previous.json`. 이번 준비 재개 근거: 같은 폴더의 `preparation-resume/authorization.json`.


## 후속 수정 결과

관측된 amount 형태를 연구 ICN→CDG KE901 일반석 KRW에만 허용했다. 총액·마일리지 정확 일치를 유지하고 원문은 수정하지 않는다. amountsMatched=False와 결제값 일치 여부를 분리 기록한다. 관련119개 T1 통과, 추가 복구/기록 회귀 통과. 교차 검토의 인계 기록 누락1건 채택·수정.

새 resume 콜백은 다음 실행용 기반이며 현재 구버전 프로세스에 적용되지 않는다. 동일 정책으로 재검증하므로 결정론적 실패를 해결하지 않으며, 유효시간 초과도 거부한다. 이번 응답은 그대로 메모리에 보존했으나 Npay 미도착이다. 이번 후속 작업에서 사이트 요청·새 주문·최종 결제는 하지 않았다.

변경 파일 SHA256:
- `travellers.py`: `b2fd292c372ce527a174c2bb902581ab6da7f970e8987866205c0508fab12f8e`
- `pipeline.py`: `ab58b57bcdb404987590fc68b508d8e3eadb81de845036071fef70002038141f`
- `state_bridge.py`: `56636913b9df764afc4bb956f78abcf1c56b7e7d0bf41e774c077f69096fa3f3`
- `connected_bridge.py`: `ad7f8f11d55ccb3fac4504e3503b8f5081fc46fed90e92edcae54d72f35843ef`
- `live_order.py`: `b0c6f606ca4b2381dce8a94d563eb52c6efc12a8284d2f8e3e04b03f40b52e39`
- `recovery.py`: `d63ab416896c0569f19d7caeacdf47b84ea90786f9c79f131bbc26eb11cc2961`
