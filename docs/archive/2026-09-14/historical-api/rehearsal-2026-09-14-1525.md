# 2026-09-14 API 주문 → 같은 주문 Npay 연결

## 결과와 범위

T2, 이미 열린 2027-09-07 ICN→CDG KE901 일반석. Windows Chrome 9232의 로그인된 기존 세션 사용. 재부팅·09시 신규 개방 시험 아님. 실행 ID `21929e2caf3a`.

**새 주문 1회 전송 후 같은 주문을 복구하여 실제 Npay checkout 확인. 최종 결제는 누르지 않았다.** 주 실행의 자동 인계는 실패했으므로 중간 개입 없는 전체 리허설 성공으로 보고하지 않는다.

## 시간과 증거

| 항목 | 관측 |
|---|---|
| API 시작 T0 | 2026-09-14 15:25:06.181 KST |
| 항공편 응답 | 요청 약1.630초, T0+1.636초 |
| 운임 응답 | 요청 약1.455초, T0+3.096초 |
| 전송 전 인계 검사 | 약11ms, 통과 |
| 주문 전송 | T0+약3.14초, 실제 요청1회 |
| 주문 응답 | 요청6.352초, T0+9.491초 |
| 자동 인계 | 15:25:19.605 session-changed, 게이트에는 이미 모델 적용 |
| 복구 후 최종 읽기 확인 | 15:42:12.661 KST, 동일 주문 Npay checkout |

수신 직후 허용 목록 증거를 저장했다. `dev-shots/state/order-evidence/21929e2caf3a/received.json`, `judgment.json`, `gate-resume-result.json` 참조. 최종 확인 방식은 `read-only-existing-window`: 이미 열린 Npay를 읽기 검증했으며 새 결제 세션을 만드는 클릭이 아니다. 같은 주문의 복구 과정에는 여러 번 결제창을 연 시도가 있었고, 신규 항공권 주문 전송은 없었다.

검증한 항목: 저장 PNR과 현재 게이트 모델 일치, 단일 여정·HK·승객 식별자 SHA256 일치, KRW349500·35000마일, 현재 게이트가 연 Npay 창, 실제 checkout 표시, NaverPay 요청 참조 일치. Npay 숫자 애니메이션의 aria-hidden 값은 복제 DOM에서 제외하고 접근성 결제금액349500원과 비교했다. HK/PNR/주문 응답 시각을 좌석 확보 시각으로 단정하지 않는다.

## 고친 부분과 남은 차이

- 승인된 실패 후 재시도는 이전 주문 증거를 보존하고 전송권 이동을 원자적으로 제한했다. 준비 중 주문 차단과 실제 주문 전송을 구분한다.
- 쿠키 문맥 비교는 관측된 bm_s/bm_sv의 무조작 회전, 정상 UI 조회 중 QueueIT/_ga 값 회전을 반영했다. 쿠키·요청·접근 통제는 변경하지 않았다. 그래도 본 실행의 게이트 이동 후 session-changed는 남았다.
- 날짜가 하위 요소를 가진 경우에도 게이트 날짜를 읽도록 보완했다.
- `resume_gate.py`는 수신 증거와 **이미 존재하는 게이트 모델**을 검증해 후반만 진행한다. 전체 응답을 파일에서 복원하거나 항공편·운임·주문을 다시 보내지 않는다.
- 마일리지 UI는 적용 완료 표시·적용취소 버튼·정확한 마일리지 숫자로 확인한다. 원래 주 실행은 특정 차감 API 응답을 기다리는 구현이 남아 있어 통합이 필요하다. UI 적용은 최종 마일리지 차감 완료 주장이 아니다.
- Npay는 연결된 label을 클릭하고 실제 radio 선택을 확인한다. 비활성화/선택 실패 시 결제하기를 누르지 않는다.
- 금액은 접근성 결제금액을 읽으며, 최종 결제 버튼 금액과 충돌하면 거부한다. 제공자 창의 최종 결제는 클릭하지 않는다.

## 검증과 다음 단계

최종 수정본 관련10개 시험 모듈 179개 통과(97.684초). 이후 결제금액 충돌 회귀1개를 추가했고 해당 ModelTest 6개 통과. 총 고유180개 통과. 실사이트 요청 없는 단위/격리 Chromium 시험이다. 실사이트 결과는 수정 중의 복구 실행과 읽기 재검증이며, 최종 코드의 중간 개입 없는 실사이트 재실행은 아직 하지 않았다.

읽기 교차 검토의 지적3건(현재 주문/회원 재검증 생략, 마일리지 부분 일치, 결제금액 출처 충돌)을 채택하여 수정했다. 최초 회귀에서는 disabled label 클릭 예외 및 단위 픽스처의 locator 누락을 발견·수정했다.

다음은 주 실행 통합 → 로컬 검증 → 승인된 일반석 자동 연속 리허설이다. 이번9.491초는 주문 응답 완료까지이며 좌석 확보나 Npay 도착 시간이 아니다. 6초 이내 확보 보장, 상대의 결제 속도/API 사용을 추정하지 않는다. 운영 브라우저 예매·계측기는 유지했다.

최종 읽기 재검토: review_recovery가 위3건 수정 확인, 해당 수정 범위 추가 결함 없음. 저장소가 Git 없는 검토 사본이므로 정식 ke_review.py scopeDigest 실행과는 구분한다.

최종 인계 코드 SHA256(실사이트 최초 실행 해시와 구분):

- `resume_gate.py`: `67a27594ab5a4b9a623b6f550de70310935862db2b83d83bc0f240c14374a461`
- `site_drive.py`: `8ae9ae602bc1c11648f367aa4ea5b0e7485277a6f137be331f5b27a41ca8b964`
- `connected_bridge.py`: `3d5a27c6289d977787c974ee001adeaa0629bd74eb3687b3cd4d99c6ecf3fc35`
- `explicit_retry.py`: `3b219ccb46a64bbf9029f33d1d45843751ed75c9af419ab5379ea47baef64839`
- `order_evidence.py`: `c6a8560a9070bc427e5979e5d8c64772c1f22c0e0c2e31145ba4167240ee10c5`
- `live_order.py`: `65899417d6e8f1e2e08daf9078aac3341b77b1e575494b73b482b96d7bbf614e`
