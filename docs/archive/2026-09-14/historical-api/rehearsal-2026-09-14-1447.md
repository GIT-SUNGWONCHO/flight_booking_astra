# 2026-09-14 14:47 수정본 일반석 리허설

## 실사이트 결과

- 사용자 새1회 승인. 9232, 2027-09-07 ICN→CDG KE901 일반석, 자체 보유마일 사용자 제공값97565. 이미 열린 날짜 T2.
- 14:43 준비는 운임 선택 실패로 중단. 실제 주문0회. 정상 운임 DOM에 잔여 `8 석` 접미사가 붙어 기존 정규식이 놓친 것이 확인됨.
- 홈페이지 탭과 예매 탭이 함께 있었다. 처음 확인한 홈페이지 탭의 로그아웃 상태를 예매 탭에 적용하면 안 된다. 실제 예매 탭은 회원 정보/로그인 유효 상태였다.
- 잔여석 접미사를 허용하고 달력으로 복귀, 미전송 준비를 동일 승인 내 재개. 14:46:21.605 시작, 14:47:03.913 준비 완료. 사이트 주문1회 차단/미차단0회.
- T0 14:47:15.147. 조회완료 +1.454초(1447ms), 운임완료 +2.794초(1334ms), 주문준비 +2.795초.
- 주문1회 HTTP200, 응답완료 14:47:24.050(+8.903초), 요청6069ms. 전송은 두 소요 차이로 약+2.83초 추정이며 별도 서버 좌석 확보 시각이 아니다.
- runId `18e1ea6cd29c`. 금액 정책 통과: amountsMatched=False, paymentAmountsMatched=True, amountLayout=observed-zero-to-total.
- 14:47:24.089 화면 인계 예외. 현재 상태 ordered / bridge-exception / paymentWindowReached=False. 여기서 ordered는 로컬 주문 응답 검증 통과를 뜻하며 예약목록 등록·좌석 확보·발권 확정이 아니다.
- Npay 미도착. 새 주문을 더 보내지 않았다. 조사 세션65637 응답 메모리 보존 중. 이 실행은 summary/exit만 가능하며 새 진단 코드가 소급 적용되지 않는다.

## 확인된 것과 미확인

- 금액 차이로 인한 이전 중단은 이번에 해결됐음.
- 실패 후 읽기 전용 조사: 대상 달력 탭1개, 준비 저장상태 검증 통과, 회원 저장정보 존재. 이것만으로 원래 cookie stamp 불일치/응답모델 오류 등 원인을 확정하지 않는다.
- 예외 세부사항을 버린 코드 때문에 실제 예외 발생 검사를 이번 실행에서 확정할 수 없음. 인계 성공으로 표현 금지.
- 전송 응답 완료8.903초를 좌석 확보 시간으로 사용하지 않는다.

## 후속 코드 보완

- 잔여석 정규식의 0석 허용은 교차 검토 지적을 채택해 양수만 허용하도록 수정. 매진/타 등급/0석 거부와 기존/8석 허용 합성 시험 추가. DOM disabled 확인은 기존 미지원이며 이 수정으로 보장하지 않음.
- 인계 예외는 자체 검증 오류의 허용 목록과 함수/줄번호만 기록. 임의 예외 원문/토큰/예약 참조는 출력하지 않는다. 함수 내부 오류 반환과 호출부 예외 모두 기록하며 금액 판정 근거도 보존한다.
- 관련19개 T1 통과. 이 후속 수정은 실행 중65637에 적용되지 않음.
- 다음: 인계 예외 원인을 진단 가능한 경로로 특정하고 같은 주문 인계 기능 검증. 추가 실사이트 주문으로 추측 검증을 반복하지 않는다.

이전 시도 기록은 retry-history/24e42d5bbad9/previous.json, 이번 미전송 준비 재개 기록은 같은 폴더의 preparation-resume/authorization.json에 보존했다. 서버 해제 확인으로 바꾸지 않았다. 운영 예매·계측기 코드와 최종 결제는 건드리지 않았다.


## 후속: 쿠키 회전에 따른 세션 오판정 재현

실제 9232 달력에서 탐색/클릭/API 전송 없이 15초 관찰했다. bm_s와 bm_sv 값이 변했고 기존 `_session_stamp` 전체 쿠키 해시도 바뀌었다. 따라서 로그인 문맥이 같은 상태에서도 `session-changed`를 유발할 수 있는 독립 결함을 재현했다. 단, 14:47 실행의 예외 내용은 남아 있지 않아 당시 직접 원인으로 확정하지 않는다.

수정은 이 두 쿠키의 값만 로그인 동일성 해시에서 제외한다. 이름/도메인/경로/존재 여부, 다른 모든 쿠키 값과 회원 저장값 비교는 유지한다. 실제 쿠키를 삭제/수정/재생하지 않으며 사이트가 사용하는 접근 통제나 전송 경로도 변경하지 않는다.

관련20개 T1 통과. 값 회전 허용, 두 쿠키 삭제 탐지, 다른 쿠키 변경 탐지 시험 포함. 읽기 전용 교차 검토에서 구체적인 회귀 지적 없음. 수정 후 실제 달력15초 관찰은 쿠키 변화가 없었으며 identityChanged=False였다. 이 후속 관찰만으로 실제 회전 후 인계 성공을 입증하지 않는다.

현재65637은 수정 전 실행으로 응답 메모리를 유지한다. 코드 수정은 소급 적용되지 않으며 인계 유효시간도 경과했다. 새 주문/취소/최종 결제 없음, Npay 미도착. 다음 실사이트 검증에서 안전한 예외 코드가 기록되어야 하며, 이번 독립 재현을 원래 실패원인 확정 또는 리허설 성공으로 보고하지 않는다.


## 후속: 주문 직전 읽기 전용 인계 점검

- `connected_bridge.preflight`를 live_order의 주문 준비 ready 뒤, 전송권 취득/실제 주문 앞에 연결했다. 로그인 문맥, binding 미사용/120초 범위, 현재 달력 URL/문서 표지, 네 저장값 일치, 목표 날짜·노선을 확인한다. 실패 시 원인 허용 목록을 남기고 주문을 보내지 않는다.
- 점검은 상태쓰기/페이지 이동/사이트 요청/기준시각 갱신을 하지 않는다. 주문 응답과 Npay를 미리 검증하는 기능은 아니며 주문 후 connect 검증은 유지한다.
- 관련67개 T1 통과. 주문 직전 점검 실패 시 조회/운임은 수행됐어도 inputTravellers 전송0회를 확인했다. 읽기 전용 교차 검토에서 구체 결함 지적 없음.
- 실제9232 달력에서 기존 문서 표지와 현재 저장 상태를 읽어 독립 점검한 결과 PASS, 20.97ms. 현재시점 비교용 문맥을 사용했으므로 기존18e1ea6cd29c의 세션/응답 재검증으로 해석하지 않는다. 사이트 요청/상태쓰기/새 주문 없음.
- 14:55:34 현재65637의 summary로 응답 메모리 보존 재확인: 총액349500/마일35000 일치. 구버전 실행이므로 현재 코드의 preflight나 새 진단을 적용할 수 없으며 Npay는 미완료다.


## 후속: 인계 실패 조사 콜백 연결

`diagnose` 읽기 문맥진단과 미사용 binding의 `resume`1회를 인계 실패/예외 양쪽에 연결했다. 원래 응답/운임/문맥을 유지하고, 재개 함수에는 새 주문이나 재조회 경로가 없다. 30초 유효시간과 주문 후 검증은 유지한다. 재개 실패 시 원래 조사로 돌아가며 중첩 조사창을 열지 않는다. 관련76개 T1 통과, 읽기 교차검토 결함 지적 없음.

이 기능은 다음 실행의 같은 프로세스 안에서만 동작한다. 기존65637의 복구, 프로세스 재시작/수정코드 반영 뒤의 응답 복원, 유효시간을 넘긴 같은 주문 재개는 구현되지 않았다. 실사이트 인계/Npay는 아직 미검증. 이번 작업에 새 주문/결제 없음.
