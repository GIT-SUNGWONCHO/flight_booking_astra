# 2026-09-09 09:00 실전 결과

**예매 실패. 실제 주문과 결제창에 도달하지 못했다. 계측은 정상 종료했다.**

후속 감사: `docs/astra-failure-audit-2026-09-09.md`. 계측은 프로그램 종료 기준 성공이었으나, 첫 목표일 요청 +5.533초의 지연과 4건의 본문 code=503 오류 누락이 추가로 확인됐다. 소진 시점 계측 성공을 뜻하지 않는다.

대상은 2027-09-04 FCO → ICN, 프레스티지 성인 1명, 배우자 계정이다. 최신 사용자 지시로 Astra가 실제 주문을 맡았으며 Claude 주문 PID 37200 트리와 Astra의 이전 dry 실행을 중단했다. 실제 실행 ID는 `20260909-085207-c8fdbeae`이다.

## 실제 시각과 결과

- 08:55: 예매/계측 독립 준비 검사 통과. 프로세스와 READY 응답을 검사했으며 신규 날짜 버튼 활성화를 입증한 검사는 아니었다.
- 09:00:00.099: 예매 실제 발사. 시스템 시계 보정 적용, 추정 불확실성 약 22ms.
- 오픈 약 +1.85초: 예매의 09-01 기준일 조회 응답에서 날짜 목록의 09-04에 일반석/프레스티지 운임이 포함되고 soldOut=false였다. 이는 날짜 목록의 운임 신호이며 좌석 수나 주문 성공 증거는 아니다.
- 그러나 실제 DOM의 09-04 버튼은 aria-disabled=true와 '-' 표시를 유지했다. recorder는 idx=2/17에서 대기했고 목표일 항공편 조회와 inputTravellers 요청은 나가지 않았다.
- 계측 첫 표본 +7.345초: 목표일 프레스티지 0석, 일반석 9석. +88.43초까지 총 20건 모두 프레스티지 0석이었다.
- 09:01:49: 진단 후 재생을 일시 중단하고 정상 날짜 버튼으로 복구를 시도했으나 09-02 역시 비활성이어서 refresh-date-unavailable로 거절됐다. 이 중단으로 예매 감독 프로그램은 실패 종료했다. 브라우저 오류 종료가 아니다.
- 최종: 예매 code=5/ok=false, 계측 code=0/ok=true, daily 전체 ok=false. 주문 생성 요청 없음, 결제창 없음, 최종 결제 승인 없음.

## 원인과 해석 한계

이번 실패의 직접 원인은 **서버 날짜 목록이 갱신돼도 화면의 목표일 버튼은 활성화되지 않는 조건을 처리하지 못한 것**이다. fire_departure_live는 비활성 목표 버튼을 허용하고 recorder가 나중에 활성화되기를 기다렸지만, 실제 화면은 갱신되지 않았다. 새 응답의 날짜와 화면의 선택 날짜가 일치하는 것만으로 충분하지 않았다. Astra가 채택한 경로의 결함이며 환경 차이 또는 매진만으로 설명하지 않는다.

이전 일반석/열린 날짜 리허설은 이 조건을 검증하지 못했다. 목표일이 날짜 띠에 존재하는 조건과 실제로 클릭 가능한 조건을 구분하는 회귀 시험과 복구 경로가 필요하다. 이 경로는 수정 및 재시험 전 실전 기본 경로로 사용하지 않는다.

첫 계측이 0석이므로 정확한 소진 시각은 미관측이다. 예매 쪽 +1.85초 날짜 목록에는 프레스티지 운임 가능 신호가 있지만, 계측기의 상세 좌석 수와 동일한 관측값은 아니다. 따라서 '6초 만에 전부 판매됐다'고 확정하지 않는다.

## 보존한 증거

모두 `dev-shots/runs/20260909-085207-c8fdbeae/` 아래에 있다.

- `manifest.json`, `checkpoint.json`: 실제 모드, 목표, 빌드 해시와 준비 판정.
- `autorun_report.json`, `macro.log`: 실제 발사와 2/17 정체, 비활성 상태, 실패 종료.
- `date_strip_evidence.json`: 시각별 날짜 목록 응답의 운임 필드와 실제 DOM 비활성 상태. 인증정보는 저장하지 않았다.
- `live_date_recovery.json`: 복구 전 상태와 실패 사유.
- `network.json`: 주문 전송이 없었던 요청 경로/시각.
- `watch_seats.json`, `watch.log`: 20건의 목표일 잔여석 관측과 소진 시각 미관측.
- `daily_report.json`: 예매 실패/계측 성공 독립 결과.
