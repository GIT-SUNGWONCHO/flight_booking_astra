# 2026-09-09 09:00 실전 준비 점검

점검일: 2026-09-09. 이 문서는 Astra 작업 폴더 기준이다.

**실전 종료 후 판정: 예매 실패, 계측 성공.** 준비 검사는 통과했지만 목표일 DOM 버튼이 활성화되지 않아 2/17에서 정체했다. 주문/결제창 없음. 상세 결과는 `docs/astra-result-2026-09-09.md`에 기록했다. 아래 준비 이력은 성공 판정으로 해석하지 않는다.

## 오늘 확정한 실행 범위

최신 사용자 지시: **Astra가 실제 주문과 결제창 진입을 담당한다.** 이전 Claude 실제 주문/Astra 부분 비교 선택은 철회됐다. 최종 결제 승인은 사용자가 한다. 계측기는 별도로 실행한다.

- 현재 실행 ID: `20260909-085207-c8fdbeae`, 발사 `2026-09-09T09:00:00+09:00`.
- FCO → ICN, 2027-09-04, 프레스티지 1명, 배우자 계정, dry=false, payment-window.
- 08:52 전환: 확인된 Claude 주문 PID 37200 트리와 Astra dry 실행 084649-767526be를 중단했다. 기존 Chrome을 유지하고 실제 모드를 시작했다.
- 08:42부터 두 프로필의 로그인/화면 준비를 진행했고 각각 성공했다. 08:46에는 같은 설정으로 daily를 수동 시작했다. 기존 브라우저를 다시 종료하지 않았다.
- 예매와 계측기를 각각 준비하며, 전환 후 08:55 검사 결과는 해당 실행의 `checkpoint.json`으로 확인한다. 09:00 발사 시각은 유지한다.
- 08:55 검사 통과: checkpoint.ok=true, failures=[], failedWorkers=[]. 예매와 계측기 모두 최근 READY 응답과 09-04/09:00 설정 일치. 준비 검사 통과이며 좌석 확보 결과는 아직 없다.
- 오늘 한 번의 09:00 대기는 프로그램이 수행한다. 반복 자동 시작 예약이 설치된 것은 아니다.
- `astra_target.ps1` 기본 모드는 payment-window이며 PartialDry만 부분 시험이다. 스크립트 시작 허용 시각 08:30~08:45는 유지하며, 오늘은 이미 준비한 브라우저에서 daily를 직접 시작했다.

아래 내용은 어제 점검 이력과 전체 예매 경로의 검증 범위이다. 오늘 실행 설정과 검사 시각은 위 내용을 우선한다.

## 판정

**독립 실행 수정과 부분 실사이트 시험 완료. 전체 실전 준비 완료 판정은 유보한다.** 계측기 오류 후에도 예매가 예정대로 발사해 주문 직전까지 진행했다. 수정한 계측기는 별도 실행에서 15건을 기록했다. 브라우저 동시 종료 원인은 미확인이며 Astra 자동 시작 예약은 미등록이다.

- 목표: 2027-09-04 FCO → ICN, 프레스티지 성인 1명, 배우자 계정.
- 실행 파일: `dev/astra_target.ps1`. 예매와 계측기를 각각 실행하며 한쪽의 준비 실패가 다른 쪽을 중단하지 않는다.
- 브라우저: 예매 `ASTRA · 9232`, 계측 `ASTRA · 9233`. 두 전용 프로필의 실행과 이름 저장 성공.
- 9232: 이번 점검에서 저장된 SKYPASS 정보로 재로그인, FCO → ICN의 08-30 조회 화면 도달, KRW 변경 및 확인 성공. 주문은 만들지 않았다. 준비 기록: `20260908-141234-deedb791/readiness.json`.
- 9233: 사용자 로그인 후 로그인/달력 준비 성공. 수정한 계측기에서 실제 표본 15건 기록, 첫 표본 +7.277초. 처음부터 프레스티지 매진으로 소진 시각은 미관측이다.
- 자동 시작: Astra 작업 미등록. 확인된 `ke_morning`, `ke_precheck`는 Claude 원본 폴더를 실행하므로 Astra를 시작해 주지 않는다.
- 사용자 요청에 따라 두 프로그램의 준비 검사를 독립적으로 처리한다. 계측기 시작/준비 실패에도 예매는 계속 실행한다. 예매 자체의 준비 검사 실패는 예매만 중단한다. 브라우저 시작 실패도 포트별로 처리한다.

## 예매 단계

1. 내일 08:35경 실행 파일을 시작한다. 실행 허용 시간은 당일 08:30 이상, 08:45 미만이다.
2. 전용 Chrome을 재시작하고 배우자 계정으로 로그인한다. FCO → ICN 편도, KRW와 2027-09-01의 날짜 목록을 준비한 뒤 08-30을 선택해 대기한다. 화면의 날짜 목록 범위는 유지한다.
3. 08:50에 각 프로그램의 프로세스 생존, 최근 준비 신호, 대상 날짜, 발사 시각을 확인한다. 실패한 프로그램만 중단하고 보고서를 남긴다. 계측기 실패는 예매 중단 사유가 아니다.
4. 09:00에 09-01을 조회해 날짜 띠를 갱신한 뒤 신규 목표 09-04를 선택한다. 목표 날짜 응답을 확인하고 프레스티지 운임 선택 → 승객/연락처 → 주문 → 결제창까지 진행한다.
5. 실제 현대카드 결제창 표시를 확인한 뒤 멈춘다. 결제창 안에서 최종 결제를 승인하지 않는다.

수동 시작 명령(PowerShell):

```powershell
& 'D:\Work\240_AI_project\99_side\flight_booking_astra\dev\astra_target.ps1'
```

현재 자동 예약이 없으므로 이 명령을 내일 허용 시간에 시작해야 한다. 지금 실행하면 날짜/시간 검사로 거부된다.

## 계측기 단계

1. 9233의 본인 네이버 연동 계정 로그인과 FCO → ICN 달력을 준비한다.
2. 08:50 준비 검사 후 08:59:57.500을 기준으로 시계 오차를 보정하여 달력 조회를 시작한다.
3. 09-04가 선택 가능해지면 해당 날짜를 조회한다. 목표 날짜 응답을 받은 뒤에만 반복 조회한다.
4. 오픈 후 20초까지 요청 간격 1초, 이후 6초를 목표로 조회하며 09:01:30까지 기록한다. 실제 응답 간격은 서버 지연에 따라 달라진다. 양수 좌석에서 매진으로 변한 응답을 관측하면 일찍 종료한다.
5. 날짜별 응답 기록 시각, 프레스티지 잔여석/매진 여부, 첫 표본 시각, 소진 변화 관측 여부를 저장한다. 예약이나 주문은 하지 않는다.

첫 표본부터 매진이면 **그 시점에는 예약 불가였음**만 뜻한다. 좌석이 열렸던 시각이나 정확한 소진 시간은 알 수 없다. 이번 수정은 표본의 상대 시각을 Python 읽기 시각 대신 브라우저 응답 기록 시각으로 계산하고, 양수 좌석을 보지 못한 경우 소진 관측으로 처리하지 않게 한다. 이는 서버 내부의 좌석 잠금 시각은 아니다.

## 확인된 시험 범위

- 기존 전체 리허설 `20260908-132652-64e3063d`: 17/17, 실제 현대카드 결제창 25.66초, 주문 요청 전송 오픈+11.286초, 응답+17.374초. 최종 결제 승인 없음.
- 리허설 통과 (실전과 다른 점: 날짜가 이미 열려 있었고 일반석이었다. 09:00 신규 개방/프레스티지 경쟁과 다름).
- 이번 재점검: runtime 15개(시작/준비 실패 격리, 준비 시간 초과 출력 보존, 일시적 파일 잠금 재시도 포함), 조회 응답 30항목, 날짜 띠 전이/월 경계 fixture 1개, 결제창 판별 fixture 1개, 핵심 회귀 8개 통과.
- `20260908-141611-479118b6`: 준비 검사 통과 후 예매는 target-not-in-strip, 계측은 쿠키 안내에 가린 날짜 버튼 때문에 실패. 날짜 간격 대조 및 발사 전 대기 날짜 선택, 모든 로그인 방식의 쿠키 동의 처리를 추가했다.
- `20260908-143255-53d37585`: 두 준비 작업이 제한시간 초과로 실패. 이후 시간 초과 직전의 출력도 보존하도록 수정했다.
- `20260908-144210-9e6cb395`: 독립 실행 코드로 동시 부분 시험. 14:43:10 두 브라우저의 종료/연결 거부 후 재실행하여 준비에 성공했다. 종료 주체는 미확인이다. Claude의 리허설도 실행 중이었지만 원인으로 단정하지 않는다.
- 같은 실행에서 계측기는 준비 신호 파일 교체 중 WinError 5로 종료됐다. 준비 검사는 `failedWorkers=[watch]`로 기록했고 **예매는 중단하지 않았다**. 예매 종료 코드 0, 6/6, dryReady=true. 보고서 seconds=11.74초이며 날짜 갱신 포함 실제 발사~보고 종료는 약 14.22초다. 주문 생성/결제창 단계는 수행하지 않았다.
- 파일 교체는 호출별 고유 임시 파일과 최대 0.31초의 짧은 재시도를 적용했다. 영구 오류는 실패로 남긴다.
- `20260908-144829-e041df8d`: 준비 완료된 9233 달력을 재사용한 읽기 전용 부분 시험. 15건, 첫 표본 +7.277초, initiallyUnavailable=true, depletionObserved=false, goneSinceOpen=null, 종료 코드 0. 예매 발사와 약 1.68초 차이로 진행됐다.
- 두 부분 시험은 이미 열린 2027-09-01/일반석 조건이며 수동 시작했다. 브라우저는 표시되었고 동일 PC/네트워크를 사용했지만 네트워크 종류는 별도 확인하지 않았다. 예매는 daily 진입, 재측정은 watch_seats 직접 진입/준비 화면 재사용이다. 신규 개방/프레스티지/OS 재부팅/예약 작업 진입은 대체하지 못한다.
- Chrome 재시작은 정상 종료 요청, 정확한 전용 프로필 경로의 잔여 프로세스 정리, 포트와 프로필 프로세스 해제 확인을 거친다. 이전 재시작 실패 기록을 성공으로 덮어쓰지 않는다.
- 6초 안에 주문 요청 전송은 달성하지 못했다. API prefetch 혼합 방식은 시험에서 더 느려 실전 후보에서 제외했다.
- OS 재부팅 후 자동 시작과 09:00 신규 개방 목표는 아직 시험하지 않았다.

## 남은 순서

1. 두 브라우저의 동시 종료 원인 확인 및 내일 실전까지 재발 방지.
2. 최신 전체 결제창 리허설 `20260908-145224-51480651`은 14:54:25 브라우저 종료로 실패했다. 오늘은 사용자가 Astra 주문 직전 비교를 선택했으며, 이를 전체 결제창 검증 완료로 표현하지 않는다.
3. 자동 시작 여부 확정. 현재는 내일 08:35 수동 시작이 필요하다.
4. Claude 쪽 실제 주문도 실행된다면 중복 주문이 생기지 않도록 실제 주문 담당을 정한다. 원본 예약 작업은 수정하지 않았다.

보고서는 `dev-shots/runs/<실행 ID>/` 아래 `manifest.json`, `checkpoint.json`, `macro.log`, `watch.log`, `autorun_report.json`, `watch_seats.json`, `daily_report.json`으로 구분한다.

한쪽만 성공하면 통합 보고서의 `ok`는 false지만, 성공한 프로그램의 `workers.macro.ok` 또는 `workers.watch.ok`는 그대로 보존한다. 통합 실패 판정은 완료된 예매를 취소하거나 성공 보고서를 실패로 덮어쓰지 않는다.
