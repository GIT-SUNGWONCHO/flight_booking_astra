# AI 작업 규칙

이 문서는 AI가 지켜야 할 작업 범위·권한·변경 규칙을 정의한다. 프로그램의 아키텍처와 운영 절차는 [README.md](README.md)를 따른다.

## 1. 작업 시작과 문서 탐색

새 계정·새 세션도 현재 로컬 문서를 기준으로 복원한다. 첫 인수인계에서는 [README의 세션 인계 절차](README.md#new-session)를 따른다. 문서 확인 요청만으로 새 주문·예약 생성·개발을 시작하지 않는다.

1. [NOW.md](NOW.md)에서 다음 목표·현재 상태·미완료 항목을 확인한다.
2. [README.md](README.md)에서 해당 구성 요소와 운영·장애 대응 절차를 읽는다.
3. [FACTS.md](FACTS.md)에서 관련 실측 근거와 해석 한계를 대조한다.
4. 실행 날짜·노선은 [공통 일정 설정](config/test_calendar.json)을 기준으로 한다. [CALENDAR.md](CALENDAR.md)는 생성본이다.

`docs/archive/`는 과거 증거이며 실행 지시가 아니다. `CLAUDE.md`·`BACKLOG.md`는 안내 문서다. 이전 `daily.py`·`autorun.py`·`watch_seats.py`의 실행법을 현재 운영과 섞지 않는다.

## 2. 작업 영역과 원본 보호

| 영역 | 허용 범위 |
|---|---|
| Astra 운영 | 이 `flight_booking_astra` 폴더. 예매 9232·`.debug-profile`, 계측 9233·`.debug-profile2` |
| API 연구 | 기본 `api_booking/`와 9242·`.api-profile`. 프로필 복사 금지. 과거 9232 시험 예외는 아래 기록 참조 |
| Claude 원본 | 옆 `flight_booking`의 파일·설정·예약 작업을 변경하거나 9222/9223에 연결하지 않음 |

Chrome을 프로세스 이름으로 일괄 종료하지 않는다. 종료가 필요하면 소유 프로필·실행 ID·PID를 확인한 해당 범위에 한정한다.

유지할 예외 승인: 원본 예약 작업 `ke_morning`·`ke_precheck`는 사용자 승인으로 사용 중지했으므로 다시 켜지 않는다. 중복 주문 방지를 위해 **확인된 Claude 실제 주문 실행 프로세스만** 중단할 수 있다는 기존 승인은 원본 코드·프로필 변경 허가가 아니다.

9232 예외: [최신 연속 리허설](api_booking/rehearsal-2026-09-14-1637.md)에 사용자 승인으로 9232에서 API 연구를 수행한 경과가 있다. 과거 사건·실패 기록은 `docs/archive/2026-09-14/historical-api/`에 보존한다. 이를 포트 선택 가능한 현재 코드의 근거와 구분해 보존하며, 향후 모든 실사이트 시험·운영 자동 교체의 포괄 승인으로 확대하지 않는다. 현재 개발은 [API 계획 §4~§5](api_booking/README.md#development)의 로컬 단계부터 진행한다.

## 3. 행동 권한과 핵심 제약

- Astra가 예매·리허설의 주문 생성과 실제 결제창 진입을 담당한다. **결제창 안의 최종 승인·결제 확정은 사용자만 한다.**
- 실전 대기 시작은 사용자가 누른다. 준비 프로그램이나 AI가 대신 무장·발사하지 않는다. 명시적인 리허설의 시험용 대기 동작은 별도다.
- 예매와 계측의 실패 처리를 분리한다. **계측의 준비·로그인·실행 실패를 이유로 예매를 중단하지 않는다.** 각 프로그램 자체의 오류와도 구분한다.
- Claude와의 병행은 별도 프로필의 주문 전 시험 범위로 다룬다. 중복 실제 주문을 만들거나 임의로 Claude 세션을 변경하지 않는다.
- API 실사이트 요청·주문은 명시적으로 허용된 시험 범위에서만 수행한다. 캡처 준비도 주문을 만들 수 있으므로 이름이 dry라는 이유만으로 무주문으로 분류하지 않는다.
- 사용자가 확정한 운영 예매를 고도화 실험으로 바꾸지 않는다. 일정·노선·동작 변경은 현재 사용자 지시와 README의 변경 기준을 따른다.

## 4. 변경과 검증

- 실행 ID·설정·빌드/실행 코드 해시·실제 시험 범위를 기록하고 같은 실행의 보고서를 읽는다.
- 리허설 성공은 README의 **실제 결제창 판정 기준**으로 보고한다. `--partial-dry` 같은 부분 점검을 전체 리허설로 표현하지 않는다.
- 로컬 시험·실사이트 리허설·09시 실조건을 구분한다. API 결과가 화면을 갱신했다고 가정하거나 오류·미개방을 P 없음으로 분류하지 않는다.
- 실행 변경은 [검증 지침](.claude/skills/ke-verify/SKILL.md)과 [README의 변경별 시험](README.md#handoff)을 적용한다. 코드 변경 없이 문서를 정리할 때는 링크·명령·코드 대조로 확인한다.

## 5. 기록과 인계

문서는 한글로 쓰고 파일명·명령·식별자는 원문을 유지한다. 계정 원문·비밀번호·토큰·결제 식별자를 기록하지 않는다.

2026-09-14 사용자 명시 저장 허용 예외: `pnr`, `orderId`, 목표 날짜·공항·항공편·운임 등급, segmentStatus, currency, totalAmount, mileage, 주문 요청/응답 시각과 확인된 생성 시각, 실행 ID, 허용 목록으로 정제한 응답 진단, 승객 식별자 지문. API 연구의 Git 제외 경로 `dev-shots/state/order-evidence/`에 저장한다. 응답 전체·승객 원문·쿠키·인증/결제 토큰·pageTicket은 포함하지 않는다. 추가 항목은 사용자 검토 후 확정한다. 식별자를 일반 문서·터미널 출력으로 복사하지 않는다.

상태와 과제는 NOW, 운영 명세는 README, 입증된 사실은 FACTS, 확정 일정은 공통 설정, API 설계는 `api_booking/README.md`에 관리한다. 긴 절차와 이력을 여러 문서에 복사하지 않는다. 작업을 마칠 때 [인계 지침](.claude/skills/ke-wrap/SKILL.md)에 따라 실제 변경 범위와 미완료를 남긴다.
